# OPTIONAL legacy Linux worker — NOT used by the Windows deployment.
FROM mcr.microsoft.com/playwright:v1.45.0-jammy
WORKDIR /app
COPY worker/package*.json ./
RUN npm install
COPY common/ ./common/
COPY worker/ ./worker/
CMD ["npx", "tsx", "worker/worker.ts"]
